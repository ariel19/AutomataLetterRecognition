#include <stdio.h>
#include <stdlib.h>
#include <time.h>

#include "types.h"
#include "matrix.h"
#include "alr.h"

int main(int argc, char **argv) {
	automata_t atm;

	puts("Hello, automata!");
	
	srand(time(NULL));
	// automata_init(&atm);
		
	return EXIT_SUCCESS;
}
